# Not used when config.yaml defines `image:` (Supervisor pulls the prebuilt image).
# Local/dev fallback only — prefer the GHCR image built from Dockerfile.addon.
ARG BUILD_FROM=ghcr.io/hiddevanbrussel/dashboard-home-assistant-addon/amd64:0.4.9
FROM ${BUILD_FROM}
